<Window x:Class="PdfHandler.UI.Views.DetailedInfoDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="詳細情報" 
        Height="650" 
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- タイトル -->
        <TextBlock Grid.Row="0"
                   Text="詳細情報"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,20"
                   Foreground="#333333"/>
        
        <!-- 本文 -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                CornerRadius="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="InfoTextBlock"
                           FontFamily="Consolas, Courier New"
                           FontSize="12"
                           TextWrapping="Wrap"
                           Padding="20"
                           LineHeight="18"
                           Foreground="#333333"/>
            </ScrollViewer>
        </Border>
        
        <!-- ボタン -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">
            <Button Content="📋 情報をコピー" 
                    Click="CopyButton_Click"
                    Padding="15,8" 
                    Margin="0,0,10,0"
                    Background="White"
                    BorderBrush="#CCCCCC"
                    Cursor="Hand"/>
            <Button Content="閉じる" 
                    Click="CloseButton_Click"
                    Padding="15,8"
                    Background="#6C757D"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
